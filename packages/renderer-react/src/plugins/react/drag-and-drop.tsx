import * as React from 'react';
import styled from 'styled-components';
const DropArea = styled.div`
  height: 20px;
  width: 100%;
`;
export function renderTopicDropArea(props) {
  return <DropArea />;
}
